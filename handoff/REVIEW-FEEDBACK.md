# Review Feedback — Step 25
Date: 2026-05-06
Ready for Builder: YES

## Must Fix
None.

## Should Fix
None.

## Escalate to Architect
None.

## Notes
- **Inline `:root` comments dropped during Slice 1.** The pre-Step-25 `:root` carried small inline rationales next to `--d0`, `--green-glow`, `--crimson` (e.g. `/* Step 21 R3 — slot inner bezel / action-button bg / seed monospace bg. */`). Bob's edit replaces the whole block with the canon-aligned version from the brief, which has no such inline comments — so those notes are gone. The brief explicitly directed "Replace the whole block in one edit" with the new structure as written, so this matches brief intent. Flagging only because the BUILD-LOG search history loses a small trace. No action requested.
- **Letter-spacing scope (3 vs 4 sites).** The brief slice 5 prose says "Four uses on the `.gb-btn*` classes" then enumerates 3 sites with the parenthetical "(There may be a 4th site — search and convert all matches.)". A grep across `pixeldex.css` confirms only 3 `.gb-btn*` blocks declare `letter-spacing: 0.03em` (`.gb-btn` line 797, `.gb-btn-primary` line 815, `.gb-btn-danger` line 833). The remaining 2 `0.03em` sites (`.team-name` line 428, `.box-cell-name` line 522) are text-label classes, not buttons. The user's review framing pre-cleared this: "if the actual count is 3, that's fine ... those are out of scope per the brief's 'on .gb-btn* classes' gloss." Bob's read aligns.
- **Test runner not exercised by Reviewer.** Bob reports `782 runs, 0 failures, 0 errors` and the new file `5 runs, 5 passes`. Reviewer attempted to run `bin/rails test test/integration/design_canon_test.rb` and `mise exec -- bundle exec rails test ...` from the worktree but the local mise/bundler chain on this machine resolves bundler 2.4.11 from a Ruby 3.0.6 install instead of the project's pinned 3.4.5, so the runner never started. Every assertion in `design_canon_test.rb` is a static-file regex match against `pixeldex.css` and `design_canon.md`, and every regex was verified by hand against the on-disk file content (lines 24, 29-31, 39-46, 222-226, 833-844, 965-969, plus `design_canon.md` lines that contain `--accent` / `--success` / `--danger`). The 5 assertions cannot fail given the file content I read. Trusting Bob's runner output.
- **Test 4's regex (`/#{Regexp.escape(sel)}\s*\{[^}]+\}/m`)** matches the first occurrence of each selector and uses a non-greedy character class that disallows `}` inside the block. The 3 selectors it tests (`.gb-flash-alert`, `.gb-btn-danger`, `.gb-status-dead`) each have rule blocks with no nested braces, so the regex correctly captures the main block (not the `:hover` variant for `.gb-btn-danger`, since `:hover` appears later in the file). Smoke-test style is appropriate for a mechanical CSS step — narrow, focused, and falsifiable; not brittle.
- **High-traffic surface spot-check.** Cross-checked the named surfaces: `.dash-r1` (only `.draft-cta` padding gained 4px horizontal), `.slot` / `.roster-card` (no diff), `.pc-box-r2 .review-tray` (no diff; only `.pc-box-r2 .badge-legend .badge` padding gained 1px horizontal), `.map-r4` (only `.map-r4 .group-card .head .pill` padding gained 1px horizontal and `.map-r4 .status-bar .jump-btn` padding gained 2px vertical), `.gb-candidate-card` + `.tcg-coin*` (no diff), `.dash-r1 .run-pill-menu` (no diff). All visual deltas are sub-pixel-class padding adjustments on small pills/buttons. No visual-regression risk on any redesigned surface.
- **Hard constraints — confirmed honored.** `git diff --stat` shows only `pixeldex.css` (CSS), `BUILD-LOG.md`, `REVIEW-REQUEST.md`, `ARCHITECT-BRIEF.md` modified, plus untracked `design_canon.md` (Architect-authored), `2026-05-06-design-canon-audit.md` (Architect-authored), `test/integration/design_canon_test.rb` (Bob-authored). Zero view files, zero controllers, zero models, zero services, zero config, zero migrations. The 9 legacy palette tokens (`--d0/--d1/--d2/--l1/--l2/--white/--amber/--green-glow/--crimson`) are preserved verbatim; the 3 border tokens (`--border/--border-thin/--border-double`) were retargeted from `var(--d1)` to `var(--ink)` (value-stable, brief explicitly shows this in the canon block). Decorative one-offs (`.team-builder-status--error` line 73, `.pc-box-r2 .box-cell.dead` line 1819, `.map-r4 .node-legend .glyph.dead` / `.map-r4 .node.dead .glyph` / `.map-r4 .acc-row .glyph.dead` lines 1193/1295/1621, `.gb-btn-danger:hover { color: #f0c0c0 }` line 848, `.tcg-coin*`, `.gb-avatar--c0..c3`) all untouched. `body { line-height: 1.8 }` line 105 and `.dialog { line-height: 1.8 }` line 759 untouched. Definition of Done — every box checkable.

## Cleared
Reviewed: `:root` block (Slice 1 — palette preserved; semantic aliases `--shadow/--ink/--shade/--moss/--canvas/--paper/--accent/--success/--danger` added pointing at the legacy tokens; danger family `--danger-bg #4a1c1c / --danger-border #6b2c2c / --danger-fg #e8a0a0` declared; spacing scale `--s-1 4px` through `--s-8 22px`; type scale `--t-micro 7px` through `--t-xl 16px`; letter-spacing `--ls-tight 0.5px / --ls-default 1px / --ls-wide 0.1em`; line-height `--lh-tight 1 / --lh-snug 1.4 / --lh-body 1.6`; border tokens retargeted to `var(--ink)` — value-stable). Slice 2 — `.gb-flash-alert` (lines 222-226) bg/color/border-color reference `var(--danger-bg/fg/border)`; `.gb-btn-danger` main block (lines 833-844) bg/color/border reference `var(--danger-bg/fg/border)`; `.gb-btn-danger:hover` (lines 846-849) only the background swapped to `var(--danger-border)` with `color: #f0c0c0` left inline as required; `.gb-status-dead` (lines 965-969) all three properties reference `var(--danger-bg/border/fg)`. Slice 3 — `padding: 2px 5px` → `padding: var(--s-1) var(--s-2)` at all 4 sites: `.map-r4 .group-card .head .pill` line 1548, `.pc-box-r2 .badge-legend .badge` line 1684, `.state-pill` line 1945, `.hof-pill` line 2124; post-edit grep confirms 0 remaining `padding: 2px 5px` and 4 new `padding: var(--s-1) var(--s-2)`. Slice 4 — `.dash-r1 .next-battle .draft-cta` line 2473 and `.map-r4 .status-bar .jump-btn` line 1162 both share `padding: var(--s-3) var(--s-5)` (= `8px 12px`). Slice 5 — `letter-spacing: var(--ls-tight)` on `.gb-btn` line 806, `.gb-btn-primary` line 824, `.gb-btn-danger` line 842; the 2 remaining `0.03em` sites (`.team-name` line 428, `.box-cell-name` line 522) are text-label classes outside the `.gb-btn*` scope per Definition of Done. Slice 6 — `test/integration/design_canon_test.rb` (5 assertions: accent alias, danger family declared, spacing scale declared, danger blocks reference `var(--danger-*)`, `design_canon.md` exists and references locked tokens) verified verbatim against the brief; every regex matches the on-disk CSS / md content. Hard constraints honored: 0 view / controller / model / service / config / migration touched; legacy palette preserved; namespaced redesigns untouched except for the 6 surgical paddings called out by the brief; decorative one-offs (Section 8) untouched. Test count claim: 777 → 782 (+5) — plausible given the 5 new assertions and consistent with Bob's claim.

**Step 25 is clear.**
